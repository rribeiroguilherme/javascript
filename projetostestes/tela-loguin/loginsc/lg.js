function login() {
    
    var us = document.getElementById('em')
    var s = document.getElementById('se')
    var u = us.value
    var ss = s.value
    if (us.value.length == 0 || s.value.length == 0) {
        window.alert('[ERRO] Preencha os dados e tente novamente!')
    } else {
        window.document.write('LETICIA NAO TA PRONTO KKKKKKKKKKKK')
    }
}